<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de Películas</title>
    <link rel='stylesheet' href='/css/style.css' />
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11" id="sweetAlert"></script>
</head>

<body>
    <% if (typeof movie != "undefined") { %>
        <script>
            Swal.fire(
            "La película se guardó satisfactoriamente",
            " ",
            'success'
            );
        </script>
       <% } %>
       <% if (typeof deleted != "undefined" && deleted == "true") { %>
           <script>
                  Swal.fire(
                  "Pelicula eliminada exitosamente!",
                  " ",
                  'success'
                  );
           </script>
          <% } %>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1 class="moviesListTitulo"></h1>
    <br>
    <p>
        <a class="botonAgregar" href="/movies/add">Agregar una Pelicula</a>
        <a class="botonVolver" href="/">inicio</a>

    </p>
    <br>
    <ul>
        <% for( let i = 0; i < movies.length; i++ ) { %>
        <li>
            <a  href="/movies/detail/<%= movies[i].id %>"><%= movies[i].title %></a>
        </li>
        <% } %>

    </ul>
    <script src="/js/moviesList.js"></script>    
</body>
</html>